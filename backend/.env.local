STAGE=local
LOG_LEVEL=INFO
JWT_SECRET_KEY=localtest-secretkey

# DB設定(rdb)
RDB_NAME=scobit_dev
RDB_HOST=localhost
RDB_PORT=15432
RDB_USER=dev
RDB_PASS=dev

# DB設定（DYNAMODB）
DB_TABLE=scobit_dev
AWS_REGION=ap-northeast-1
DYNAMO_ENDPOINT=http://localhost:28000
